import { PlateFormatterPipe } from './plate-formatter.pipe';

describe('PlateFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlateFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
